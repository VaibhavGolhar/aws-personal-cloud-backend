<configuration>
    <!-- Generate run timestamp at startup -->
    <timestamp key="RUN_TIME" datePattern="yyyyMMdd-HHmmss"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender: logs stored in logs/app-<timestamp>.log -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app-${RUN_TIME}.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger: write to console + file -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>